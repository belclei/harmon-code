{
  "name": "lurem",
  "private": true,
  "workspaces": ["packages/*", "apps/*"],
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "lint": "biome check .",
    "generate": "npm run generate --workspace=@lurem/db --if-present",
    "typecheck": "npm run generate --workspace=@lurem/db --if-present && npm run typecheck --workspaces --if-present",
    "test": "npm run test --workspaces --if-present",
    "boundaries": "depcruise --config .dependency-cruiser.cjs packages $([ -d apps ] && echo apps)",
    "storybook": "npm run storybook --workspace=@lurem/ui",
    "build-storybook": "npm run build-storybook --workspace=@lurem/ui",
    "dev": "concurrently -n api,web -c blue,green \"npm run dev --workspace=@lurem/api\" \"npm run dev --workspace=@lurem/web\""
  },
  "devDependencies": {
    "@biomejs/biome": "^1.9.4",
    "@types/node": "^20.17.9",
    "concurrently": "^9.1.0",
    "dependency-cruiser": "^16.10.0",
    "typescript": "^5.7.2"
  }
}
