# Copy to .env for local dev (Vite reads it automatically). Public by
# design — same value as apps/api/.env's GOOGLE_CLIENT_ID, just also baked
# into the frontend bundle so the Google Sign-In button can initialize.
VITE_GOOGLE_CLIENT_ID="replace-with-real-google-oauth-client-id.apps.googleusercontent.com"
